package org.einnovator.devops.client.modelx;

import org.einnovator.devops.client.model.ConfigMap;
import org.einnovator.util.model.EntityOptions;
import org.einnovator.util.model.ToStringCreator;

/**
 * Options to retrieve {@code ConfigMap}s.
 *
 * @see ConfigMap
 * @author support@einnovator.org
 *
 */
public class ConfigMapOptions extends EntityOptions<ConfigMap> {


	public ConfigMapOptions() {
	}

	@Override
	public ToStringCreator toString(ToStringCreator creator) {
		return super.toString(creator)
				;
	}
}
